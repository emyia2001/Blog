---
title: "锁住的示例"
date: 2026-09-08
type: "diary"
lock:
  question: "这是一条加密示例。输入答案 snark 即可解锁查看正文。"
  answer: "snark"
---

这是加密正文。构建时它已被 AES-GCM 加密，页面上只会有谜面和密文，看不到这段字；只有在上面输入正确答案 `snark` 才能解密看到这段文字。

实际操作里，建议把 `answer` 设成**足够长、足够独特**的一句话（比单个单词更安全），避免用生日、姓名等容易被猜中的内容。别担心——这段明文不会以任何形式出现在站点源码或构建产物里。